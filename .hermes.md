# Hermes-Specific Rules for Bess Project

## Profile
- ใช้ profile: `bess`
- ถ้ายังไม่มี profile bess → สร้างด้วย `hermes profile create bess`

## Startup
- ทุกครั้งที่เข้า project นี้ ให้เบส:
  1. อ่าน `AGENTS.md` ก่อน (Hermes ทำอัตโนมัติ)
  2. เช็ค `memory.md` ว่ามี facts สำคัญอะไรที่ต้องรู้
  3. เช็ค `sessions/` ว่า session ล่าสุดทำอะไรค้างไว้

## Skills
- Skills ที่เบสสร้างหรือใช้บ่อย → เก็บใน `skills/` folder
- ใช้ `skill_manage` สร้าง skills และเก็บ source ใน project นี้ด้วย

## Backup Routine
- ทุกครั้งที่จบ session สำคัญ → อัปเดต `memory.md`
- Skills ใหม่ → copy ไป `skills/` folder
